# Culture to Cash account credentials (for API auth)
C2C_EMAIL=nick@culturetocash.com
C2C_PASSWORD=your-password

# API server URL (Railway URL in production)
API_URL=http://localhost:3001

# AES-256 encryption key (same as server, 64-char hex)
ENCRYPTION_KEY=same-as-server-encryption-key

# Claude API key
CLAUDE_API_KEY=sk-ant-api03-...

# Chrome/Chromium executable path (leave empty to auto-detect)
CHROME_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome

# Optional: headless mode and slow motion for debugging
HEADLESS=false
SLOW_MO=0
